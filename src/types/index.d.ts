export type TTask = {
    _id: string | number,
    taskName: string,
    category: string,
    description: string,
    dueDate: string,
    email: string,
    createdAt: string,
    updatedAt: string,
    status: string
}